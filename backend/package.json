{
  "name": "yaarxiv-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon --watch src -e ts,tsx --exec ts-node -r tsconfig-paths/register src/index.ts",
    "lint": "tsc --noEmit && eslint \"src/**/*.{js,ts}\" --quiet --fix",
    "serve": "cross-env NODE_ENV=production node out/src/index.js",
    "build-ts": "ts-node scripts/cleanOut.ts && cross-env NODE_ENV=production tsc && tscpaths -p tsconfig.json -s ./src -o ./out",
    "build": "npm run lint && npm run build-ts && npm run copy-static-assets",
    "start": "npm run dev",
    "copy-static-assets": "ts-node scripts/copyStaticAssets.ts",
    "compile-schemas": "ts-node scripts/generateSchemas.ts",
    "test": "cross-env NODE_ENV=test jest --verbose",
    "migration:create": "cross-env NODE_ENV=development ts-node ./node_modules/.bin/typeorm migration:create",
    "migration:generate": "cross-env NODE_ENV=development ts-node ./node_modules/.bin/typeorm migration:generate",
    "migration:run": "cross-env NODE_ENV=development ts-node ./node_modules/.bin/typeorm migration:run",
    "migration:revert": "cross-env NODE_ENV=development ts-node./node_modules/.bin/typeorm migration:revert"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sinclair/typebox": "^0.10.1",
    "fastify": "^3.1.1",
    "fastify-jwt": "^2.1.2",
    "fastify-swagger": "^3.1.2",
    "fastify-typeorm-plugin": "^2.1.2",
    "reflect-metadata": "^0.1.13",
    "sqlite3": "^5.0.0",
    "typeorm": "^0.2.25",
    "yaarxiv-api": "file:../api"
  },
  "devDependencies": {
    "@types/jest": "^26.0.7",
    "@types/pino": "^6.3.0",
    "@types/shelljs": "^0.8.8",
    "@typescript-eslint/eslint-plugin": "^3.7.1",
    "@typescript-eslint/parser": "^3.7.1",
    "cross-env": "^7.0.2",
    "eslint": "^7.5.0",
    "jest": "^26.1.0",
    "nodemon": "^2.0.4",
    "pino-pretty": "^4.1.0",
    "shelljs": "^0.8.4",
    "ts-jest": "^26.1.4",
    "ts-json-schema-generator": "^0.70.2",
    "ts-node": "^8.10.2",
    "tsconfig-paths": "^3.9.0",
    "tscpaths": "0.0.9",
    "typescript": "^3.9.7"
  }
}
